name: main
on:
  - push
jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@main
      - run: cp -r . ~/.dotfiles
      - run: cp ~/.dotfiles/profile ~/.profile
      - run: update-homebrew
        shell: bash -el {0}
      - run: rcup -f
        shell: bash -el {0}
      - run: update
        shell: bash -el {0}
