#!/usr/bin/env zsh

set -e

brew install getantibody/tap/antibody

antibody bundle <~/.config/zsh_plugins.txt >~/.cache/zsh_plugins.sh

if [ -d ~/.cache/antibody ]; then
  cd ~/.cache/antibody
else
  cd ~/Library/Caches/antibody
fi

for script in **/*.zsh; do
  zcompile "$script"
done
