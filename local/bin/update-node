#!/bin/sh

set -e

for subcommand in install update; do
  npm $subcommand --global --ignore-scripts \
    assemblyscript \
    diff-so-fancy \
    prettier \
    typescript \
    typescript-language-server
done
