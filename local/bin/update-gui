#!/bin/sh

set -e

brew install fontconfig # for alacritty

if ! which alacritty; then
	cargo install --git https://github.com/jwilm/alacritty
fi

cargo install-update -i way-cooler
cargo install-update -ga
